# Friday
Your ai friend.

```bash
git init
git config user.name "najmus-sakib-hossain"
git config user.email "manfromexistence@proton.me"
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/najmus-sakib-hossain/friday.git
git push -u origin main
```
